시간복잡도 Big-O는 학계에서는 시간의 상한을 의미한다고 한다.

여기서 학계에서라는 말을 넣은 이유는 복잡도를 나타내는 방식이 3가지가 있기 때문이다.

3가지를 살펴보자면

1. O(Big-Oh) 표기법
O(Big-O), O(Big-Oh)표기는 복잡도의 점근적 상한을 말한다.
여기서 말하는 점근적 상한이란?

점근적은 점점 가까워지는 모양을 말한다.
상한은 상한선을 뜻한다. 즉 O(Big-Oh)로 표기한 것 보다 더 큰 복잡도가 나올 수 없다는 것이다.

2. Ω(Big-Omega)
Ω(Big-Omega) 표기는 복잡도의 점근적 하한을 말한다.
O(Big-Oh)의 개념과 점근적이라는 것과 한계를 두고 있다는 것은 같지만
하한 즉 Ω(Big-Omega) 표기보다 낮은 복잡도는 나올 수 없다는 것을 알려준다.

3. θ (Big-Theta)
θ (Big-Theta)는 O(Big-Oh)와 Ω(Big-Omega)의 표기가 같을 때 사용한다.
즉 1,2번 두개의 표기가 겹치는 그 부분을 말한다.
왜냐하면 O(Big-Oh) 표기는 말 그대로 상한선이기 때문에 더 큰 복잡도로 표기할 때는 상한으로 제한이 없고
반대로 Ω(Big-Omega) 표기는 하한선이라 하한으로 제한이 없기 때문에
너무 큰 복잡도나 너무 작은 복잡도로 표기하여도 맞다고 하면 할 말이 없다.

그래서 이 두 가지를 혼용한 θ (Big-Theta)가 쓰인다.

결국 θ (Big-Theta)는 O(Big-Oh)와 Ω(Big-Omega) 두 가지 모두 만족한다.

여기서 주의해야 할 점은 θ (Big-Theta)가 O(Big-Oh)와 같은 표기인 O(n) 으로 표기를 한다.
헷갈리지 않도록 주의하자.

그래서 우리가 보통 보는 시간복잡도 O(N) 이런것들은 세타 표기법일 정도로 세타 표기법이 가장 보편으로 쓰인다.

마지막으로 최선 최악의 경우와 big-O, Ω, θ 와는 아무런 관련이 없으므로 이 점도 헷갈리지 않도록 주의하자.
